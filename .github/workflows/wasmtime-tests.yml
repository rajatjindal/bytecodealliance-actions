name: wasmtime
on:
  pull_request:
    branches: [main]

  push:
    branches: [main]

  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: npm install
        run: npm install

      - name: npm run build
        run: npm run package
        env:
          NODE_OPTIONS: --openssl-legacy-provider

      - name: upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: setup-wasmtime
          path: dist/wasmtime/setup/index.js

  setup-default:
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v6

      - name: Retrieve saved Github action
        uses: actions/download-artifact@v8
        with:
          name: setup-wasmtime
          path: dist/wasmtime/setup/

      - name: setup wasmtime
        uses: ./wasmtime/setup/

      - name: verify wasmtime setup
        run: wasmtime --version

  setup-version:
    needs: build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        version: ["12.0.0", "17.0.0", "dev"]
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v6

      - name: Retrieve saved Github action
        uses: actions/download-artifact@v8
        with:
          name: setup-wasmtime
          path: dist/wasmtime/setup/

      - name: setup wasmtime
        uses: ./wasmtime/setup/
        with:
          version: ${{ matrix.version }}

      - name: setup wasmtime with v
        uses: ./wasmtime/setup/
        if: ${{ matrix.version != 'dev' }}
        with:
          version: v${{ matrix.version }}

      - name: wasmtime setup successful
        if: ${{ contains(env.WASMTIME_VERSION, matrix.version) }}
        run: echo "wasmtime setup successful"

      - name: wasmtime setup failed
        if: ${{ matrix.version != 'dev' && !contains(env.WASMTIME_VERSION, matrix.version) }}
        run: |
          echo "expected version: ${{ matrix.version }}, found: ${{ env.WASMTIME_VERSION }}"
          exit 1
